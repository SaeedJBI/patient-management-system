from django.urls import path
from . import views

app_name = 'patients'

urlpatterns = [
    # We'll add views here in later stages
    # path('', views.PatientListView.as_view(), name='list'),
    # path('<uuid:pk>/', views.PatientDetailView.as_view(), name='detail'),
]